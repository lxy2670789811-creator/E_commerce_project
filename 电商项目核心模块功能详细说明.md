# 电商项目核心模块功能详细说明

> 项目技术栈：JDK 17 + Spring Boot 3.x + MyBatis-Plus + Redis + Redisson + DeepSeek AI + Knife4j(SpringDoc OpenAPI)
>
> 包路径：`com.ecommerce`

---

## 一、controller 层 — 控制器模块

### 1.1 ProductController（商品模块控制器）

**路径**：`/product`

**接口列表**：

| 方法 | 路径 | 功能说明 | 关键设计 |
|------|------|---------|---------|
| POST | `/add` | 新增商品 | 入参校验 `@Valid`，返回新商品ID |
| PUT | `/update` | 修改商品 | 更新后**主动删除Redis缓存**，保证数据一致性 |
| DELETE | `/delete` | 删除商品 | 逻辑删除（MyBatis-Plus `@TableLogic`），同时清缓存 |
| PUT | `/status` | 商品上下架 | 仅更新 status 字段，状态变更后清缓存 |
| GET | `/detail` | 商品详情 | **优先走Redis缓存**，缓存过期时间默认1小时，缓存异常降级查DB |
| GET | `/list` | 商品列表 | 支持按关键字(模糊匹配)、分类、状态筛选，按创建时间倒序 |
| GET | `/stock` | 库存查询 | 返回当前库存数量 + 是否有货标志 |

**设计特点**：
- 商品详情采用 Cache-Aside 模式：先查缓存 -> 未命中查DB -> 回写缓存
- 所有写操作（增/删/改/上下架/库存变更）后均主动删除缓存
- Redis 操作全部包裹 try-catch，缓存异常不影响主业务流程

### 1.2 OrderController（订单模块控制器）

**路径**：`/order`

**接口列表**：

| 方法 | 路径 | 功能说明 | 关键设计 |
|------|------|---------|---------|
| POST | `/create` | 创建订单 | **Redisson分布式锁 + DB乐观扣减，双层防超卖** |
| GET | `/list` | 订单列表 | 按用户ID + 可选状态筛选，按创建时间倒序 |
| GET | `/detail` | 订单详情 | 需同时校验订单ID和用户ID（防止越权访问） |
| POST | `/cancel` | 取消订单 | 仅待支付/已支付可取消；自动回滚库存（同样加分布式锁） |

**设计特点**：
- 下单流程是项目核心亮点：**Redisson 按商品ID加分布式锁** + **数据库层面 `stock >= quantity` 原子判断**，双重保险防超卖
- 取消订单同样加分布式锁，避免与并发下单冲突导致库存错乱
- 订单号生成规则：`ORD + yyyyMMddHHmmss + 6位雪花ID后缀`

### 1.3 UserController（用户模块控制器）

**路径**：`/user`

**接口列表**：

| 方法 | 路径 | 功能说明 | 关键设计 |
|------|------|---------|---------|
| POST | `/login` | 模拟登录 | 极简实现：用户名+密码校验，返回模拟Token（`mock-token-` + UUID前16位） |
| GET | `/address/list` | 查询地址列表 | 默认地址在前，按创建时间倒序 |
| POST | `/address/add` | 新增收货地址 | 设为默认时自动取消该用户其他默认地址 |

**设计特点**：
- 用户模块为极简实现，重点在订单和商品模块
- 测试账号：`test001 / 123456`
- 地址模块有完整的默认地址互斥逻辑

### 1.4 AiAfterSupportController（AI售后智能分析控制器）

**路径**：`/ai/after-support`

**接口列表**：

| 方法 | 路径 | 功能说明 | 关键设计 |
|------|------|---------|---------|
| POST | `/analyze` | 智能售后分析 | 调用DeepSeek大模型分析用户反馈，含超时/降级/限流三重保护 |

**完整流程**：
1. 根据订单ID查询数据库，获取商品名称/下单时间/支付金额/订单状态
2. 组装 Prompt（订单业务数据 + 用户反馈）调用 DeepSeek API
3. 大模型返回结构化JSON（问题分类、情绪、建议操作）
4. 解析结果并存入 `ai_after_support` 表，返回给前端

**保护机制**：
- **超时控制**：RestTemplate 配置30秒超时
- **调用失败降级**：AI失败时返回"待人工审核"，不影响接口整体可用性
- **简单限流**：基于Redis滑动窗口计数器，默认每用户每分钟10次

---

## 二、service 层 — 业务逻辑模块

### 2.1 ProductService / ProductServiceImpl

**核心方法**：

| 方法 | 功能 | 关键实现 |
|------|------|---------|
| `addProduct` | 新增商品 | ProductConvert DTO转DO，调用 save |
| `updateProduct` | 修改商品 | 先校验存在性，更新后删除缓存 |
| `deleteProduct` | 删除商品 | 逻辑删除，删除后清缓存 |
| `updateStatus` | 上下架 | 仅改 status 字段，状态变更后清缓存 |
| `getProductDetail` | 商品详情 | **Cache-Aside 模式**：Redis -> DB -> 回写缓存 |
| `listProducts` | 商品列表 | LambdaQueryWrapper 动态条件拼接 |
| `getProductStock` | 库存查询 | 直接查DB，返回库存和有货标志 |
| `decreaseStock` | 扣减库存 | 调用 Mapper 原子SQL，返回影响行数 |
| `increaseStock` | 回滚库存 | 调用 Mapper 原子SQL，返回影响行数 |

**缓存策略**：
- 缓存 Key 前缀：`ecommerce:product:detail:`
- 过期时间：默认 3600 秒（可通过 `ecommerce.cache.product-detail-expire-seconds` 配置）
- 缓存异常全部 try-catch 降级，不影响主流程

### 2.2 OrderService / OrderServiceImpl

**核心方法**：

| 方法 | 功能 | 关键实现 |
|------|------|---------|
| `createOrder` | 创建订单 | **核心防超卖逻辑**，见下方详解 |
| `listOrders` | 订单列表 | 按用户ID + 状态筛选 |
| `getOrderDetail` | 订单详情 | 校验订单归属（防止越权） |
| `cancelOrder` | 取消订单 | 状态校验 + 分布式锁回滚库存 + 更新状态 |
| `getOrderByIdForAi` | AI查询订单 | 供AI模块调用，不做用户归属校验 |

**下单防超卖核心流程（createOrder）**：

```
1. 锁外预校验（减少锁内逻辑）
   ├─ 用户存在性校验
   ├─ 地址存在性+归属校验
   └─ 商品存在性+上架状态校验

2. 获取 Redisson 分布式锁（按商品ID维度）
   ├─ 锁Key: ecommerce:lock:stock:product:{productId}
   ├─ 等待时间: 5秒
   └─ 租约时间: 30秒

3. 锁内二次校验 + 库存扣减
   ├─ 二次检查库存（防止锁等待期间库存变化）
   └─ DB原子扣减（stock >= quantity 判断，影响行数=1成功，=0失败）

4. 构建订单 + 保存
   ├─ 订单号: ORD + yyyyMMddHHmmss + 6位雪花ID
   ├─ 商品名称/价格快照（防止后续商品变更影响历史订单）
   └─ 地址快照（防止后续地址变更影响历史订单）

5. finally 释放锁（仅当前线程持有锁时释放）
```

**取消订单流程**：
- 状态校验：只有待支付(0)和已支付(1)可取消
- 同样加分布式锁回滚库存，避免与并发下单冲突
- 更新订单状态为已取消，记录取消时间和原因

### 2.3 AiAfterSupportService / AiAfterSupportServiceImpl

**核心方法**：`analyze(AiAfterSupportDTO dto)`

**完整处理流程**：

```
1. 查询订单 + 校验用户归属
2. 限流检查（Redis滑动窗口计数器）
   └─ 超出限流直接抛 AI_RATE_LIMIT_EXCEEDED 异常
3. 组装订单业务信息文本（Prompt上下文）
4. 调用 DeepSeekClient.analyzeAfterSupport()
   └─ 内部已做超时/异常捕获，失败返回 null
5. 判定是否降级
   ├─ 成功：使用AI返回的结构化结果
   └─ 失败：使用降级默认值（问题类型=其他，情绪=中性，建议=待人工审核）
6. 持久化到 ai_after_support 表
   └─ 同时存储完整AI原始JSON（ai_result字段），便于审计
7. 返回 VO
```

**降级策略**：
- AI 调用失败不抛异常，返回默认"待人工审核"结果
- 失败原因区分："AI服务调用失败或响应异常" / "AI返回字段缺失"

### 2.4 UserService / UserServiceImpl

**核心方法**：

| 方法 | 功能 | 关键实现 |
|------|------|---------|
| `login` | 模拟登录 | 按用户名查询，密码明文比对，校验用户状态 |
| `getUserById` | 根据ID获取用户 | 不存在抛 USER_NOT_FOUND 异常 |

**注意**：密码为明文存储和比对，仅用于演示，生产环境应使用加密+盐。

### 2.5 UserAddressService / UserAddressServiceImpl

**核心方法**：

| 方法 | 功能 | 关键实现 |
|------|------|---------|
| `listByUserId` | 查询用户地址列表 | 默认地址优先倒序 + 创建时间倒序 |
| `addAddress` | 新增地址 | 设为默认时先取消该用户其他默认地址 |
| `getByIdAndUserId` | 校验归属查询 | 同时按地址ID+用户ID查询，防止越权 |

**默认地址互斥逻辑**：新增地址时如果 isDefault=1，先将该用户所有已有默认地址取消。

---

## 三、entity 层 — 数据模型模块

### 3.1 BaseDO（实体基类）

**通用字段**：

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | Long | 主键，自增 |
| `createTime` | LocalDateTime | 创建时间，MyBatis-Plus 插入自动填充 |
| `updateTime` | LocalDateTime | 更新时间，MyBatis-Plus 插入/更新自动填充 |
| `deleted` | Integer | 逻辑删除标志：0-未删除 1-已删除 |

**设计特点**：
- 使用 `@TableLogic` 实现逻辑删除
- 使用 `@TableField(fill = FieldFill.INSERT/INSERT_UPDATE)` 自动填充时间
- 所有业务实体（除 AI 售后表外）均继承此类

### 3.2 ProductDO（商品实体）

**表名**：`product`

| 字段 | 类型 | 说明 |
|------|------|------|
| `name` | String | 商品名称 |
| `description` | String | 商品描述 |
| `price` | BigDecimal | 商品价格 |
| `stock` | Integer | 库存数量 |
| `status` | Integer | 状态：1-上架 0-下架 |
| `category` | String | 分类 |
| `imageUrl` | String | 商品图片URL |

### 3.3 OrderDO（订单实体）

**表名**：`orders`

| 字段 | 类型 | 说明 |
|------|------|------|
| `orderNo` | String | 订单号 |
| `userId` | Long | 用户ID |
| `productId` | Long | 商品ID |
| `productName` | String | 商品名称（下单时快照） |
| `productPrice` | BigDecimal | 商品单价（下单时快照） |
| `quantity` | Integer | 购买数量 |
| `totalAmount` | BigDecimal | 订单总金额 |
| `addressId` | Long | 收货地址ID |
| `addressSnapshot` | String | 收货地址快照 |
| `status` | Integer | 订单状态（见 OrderStatusEnum） |
| `payTime` | LocalDateTime | 支付时间 |
| `shipTime` | LocalDateTime | 发货时间 |
| `finishTime` | LocalDateTime | 完成时间 |
| `cancelTime` | LocalDateTime | 取消时间 |
| `cancelReason` | String | 取消原因 |

**设计特点**：
- 下单时保存商品名称/价格快照和地址快照，防止后续商品或地址变更影响历史订单
- 订单状态流转：待支付 -> 已支付 -> 已发货 -> 已完成；待支付/已支付 -> 已取消

### 3.4 UserDO（用户实体）

**表名**：`sys_user`

| 字段 | 类型 | 说明 |
|------|------|------|
| `username` | String | 用户名 |
| `password` | String | 密码（明文，仅演示用） |
| `nickname` | String | 昵称 |
| `phone` | String | 手机号 |
| `status` | Integer | 状态：1-正常 0-禁用 |

### 3.5 UserAddressDO（用户地址实体）

**表名**：`user_address`

| 字段 | 类型 | 说明 |
|------|------|------|
| `userId` | Long | 用户ID |
| `receiver` | String | 收货人姓名 |
| `phone` | String | 收货人手机号 |
| `province` | String | 省份 |
| `city` | String | 城市 |
| `district` | String | 区县 |
| `detail` | String | 详细地址 |
| `isDefault` | Integer | 是否默认：1-是 0-否 |

### 3.6 AiAfterSupportDO（AI售后分析记录实体）

**表名**：`ai_after_support`

**注意**：该表不继承 BaseDO，不使用逻辑删除，保留独立的创建时间字段。

| 字段 | 类型 | 说明 |
|------|------|------|
| `orderId` | Long | 订单ID |
| `orderNo` | String | 订单号 |
| `userId` | Long | 用户ID |
| `userInput` | String | 用户反馈文本 |
| `aiResult` | String | 大模型返回结果（JSON字符串） |
| `problemType` | String | 问题分类：质量问题/物流问题/咨询/其他 |
| `emotion` | String | 情绪：负面/中性/正面 |
| `suggestion` | String | 建议处理方案 |
| `aiStatus` | Integer | AI处理状态：1-成功 0-失败待人工 |
| `failReason` | String | AI失败原因 |
| `createTime` | LocalDateTime | 创建时间 |

---

## 四、dto 层 — 数据传输对象

### 4.1 product 包

| DTO | 用途 | 关键字段 |
|-----|------|---------|
| `ProductAddDTO` | 新增商品请求 | name, description, price, stock, status, category, imageUrl |
| `ProductUpdateDTO` | 修改商品请求 | id + 同上全部字段 |
| `ProductStatusDTO` | 上下架请求 | id, status |

**校验注解**：`@NotBlank`、`@NotNull`、`@DecimalMin("0.01")`（价格）、`@Min(0)`（库存）

### 4.2 order 包

| DTO | 用途 | 关键字段 |
|-----|------|---------|
| `OrderCreateDTO` | 创建订单请求 | userId, productId, quantity, addressId |
| `OrderCancelDTO` | 取消订单请求 | orderId, userId, cancelReason |

**校验注解**：`@NotNull`、`@Min(1)`（购买数量最少为1）

### 4.3 user 包

| DTO | 用途 | 关键字段 |
|-----|------|---------|
| `UserLoginDTO` | 登录请求 | username, password |
| `UserAddressAddDTO` | 新增地址请求 | userId, receiver, phone, province, city, district, detail, isDefault |

### 4.4 ai 包

| DTO | 用途 | 关键字段 |
|-----|------|---------|
| `AiAfterSupportDTO` | AI售后分析请求 | orderId, userId, userInput |

---

## 五、vo 层 — 视图对象

### 5.1 product 包

| VO | 用途 | 关键字段 |
|----|------|---------|
| `ProductVO` | 商品详情/列表视图 | 商品全部信息 + statusText（状态文本） |
| `ProductStockVO` | 库存查询视图 | productId, productName, stock, hasStock, status, statusText |

### 5.2 order 包

| VO | 用途 | 关键字段 |
|----|------|---------|
| `OrderVO` | 订单列表视图 | 订单基本信息 + statusText（状态文本） |
| `OrderDetailVO` | 订单详情视图 | 继承 OrderVO + addressId, addressSnapshot, shipTime, finishTime, cancelTime, cancelReason, updateTime |

**设计特点**：OrderDetailVO 继承 OrderVO，复用列表字段，扩展详情特有字段。

### 5.3 user 包

| VO | 用途 | 关键字段 |
|----|------|---------|
| `UserLoginVO` | 登录结果 | userId, username, nickname, token |
| `UserAddressVO` | 地址视图 | 地址全部信息 + fullAddress（省市区+详情拼接） |

### 5.4 ai 包

| VO | 用途 | 关键字段 |
|----|------|---------|
| `AiAnalysisResultVO` | AI分析结果 | id, orderId, orderNo, userInput, problemType, emotion, suggestion, aiStatus, aiStatusText, failReason, createTime |
| `AiStructuredResult` | AI结构化内部类 | problem_type, emotion, suggestion（与大模型返回JSON对应） |

**设计特点**：
- `AiStructuredResult` 是静态内部类，用于接收大模型返回的JSON
- 使用 `@JsonProperty` 映射 snake_case 到 camelCase

---

## 六、common 层 — 通用组件

### 6.1 Result<T>（统一响应结果封装）

**字段**：`code`（状态码）、`message`（消息）、`data`（数据）、`timestamp`（时间戳）

**静态方法**：
- `success()` / `success(T data)` / `success(String message, T data)`
- `fail(ErrorCode)` / `fail(int code, String message)` / `fail(ErrorCode, String message)`
- `isSuccess()`：判断是否成功

### 6.2 ErrorCode（全局错误码枚举）

**错误码分段**：

| 码段 | 含义 | 示例 |
|------|------|------|
| 0 | 成功 | SUCCESS |
| 400-499 | HTTP标准错误 | PARAM_ERROR(400), UNAUTHORIZED(401), FORBIDDEN(403), NOT_FOUND(404) |
| 1000-1999 | 商品业务错误 | PRODUCT_NOT_FOUND(1001), PRODUCT_OFF_SHELF(1002), PRODUCT_STOCK_INSUFFICIENT(1003) |
| 2000-2999 | 订单业务错误 | ORDER_NOT_FOUND(2001), ORDER_STATUS_ERROR(2002), ORDER_CANCEL_FAILED(2003), ORDER_CREATE_FAILED(2004) |
| 3000-3999 | 用户业务错误 | USER_NOT_FOUND(3001), USER_LOGIN_FAILED(3002), ADDRESS_NOT_FOUND(3003) |
| 5000-5999 | 系统错误 | SYSTEM_ERROR(5000), CACHE_ERROR(5001), LOCK_ACQUIRE_FAILED(5002), AI_RATE_LIMIT_EXCEEDED(5101), AI_CALL_FAILED(5102) |

### 6.3 BusinessException（自定义业务异常）

**构造方式**：
- `BusinessException(ErrorCode)`：使用错误码枚举
- `BusinessException(ErrorCode, String message)`：错误码 + 自定义消息
- `BusinessException(int code, String message)`：自定义码+消息
- `BusinessException(String message)`：默认系统错误码

### 6.4 GlobalExceptionHandler（全局异常处理器）

**处理的异常类型**：

| 异常类型 | 处理方式 |
|---------|---------|
| `BusinessException` | 业务异常，返回对应错误码和消息 |
| `MethodArgumentNotValidException` | @RequestBody 参数校验失败，拼接所有字段错误 |
| `ConstraintViolationException` | @RequestParam 参数校验失败 |
| `BindException` | form表单参数绑定失败 |
| `MissingServletRequestParameterException` | 缺少必填参数 |
| `MethodArgumentTypeMismatchException` | 参数类型不匹配 |
| `HttpMessageNotReadableException` | 请求体JSON解析失败 |
| `Exception` | 兜底异常，返回系统错误 |

**设计特点**：所有异常统一返回 `Result<Void>` 格式，保证前端响应结构一致。

---

## 七、ai 层 — AI相关功能

### 7.1 DeepSeekClient（DeepSeek大模型API客户端）

**核心方法**：`analyzeAfterSupport(orderInfo, userFeedback)`

**技术要点**：

1. **API调用方式**：兼容 OpenAI Chat Completions 规范，使用 RestTemplate POST 请求
2. **强制JSON输出**：设置 `response_format: { type: "json_object" }`，确保大模型返回合法JSON
3. **低温度**：`temperature = 0.1`，输出更稳定
4. **System Prompt 设计**：
   - 严格限定 problem_type 枚举值：质量问题/物流问题/咨询/其他
   - 严格限定 emotion 枚举值：负面/中性/正面
   - suggestion 限制30字以内
   - 强制只输出JSON，不含markdown标记
5. **User Prompt 设计**：【订单业务信息】+【用户反馈内容】+ 输出要求
6. **异常处理**：分层捕获
   - `HttpClientErrorException`（4xx客户端错误）
   - `HttpServerErrorException`（5xx服务端错误）
   - `ResourceAccessException`（超时/网络问题）
   - `Exception`（兜底：解析异常等）
   - 所有异常均返回 `null`，由上层做降级处理

**配置项**：
- `deepseek.api.url`：API地址
- `deepseek.api.key`：API密钥
- `deepseek.api.model`：模型名称，默认 `deepseek-chat`

### 7.2 AiRateLimiter（AI服务限流器）

**实现方式**：基于 Redis + Lua 脚本的滑动窗口计数器

**核心方法**：`tryAcquire(Long userId)`

**技术要点**：

1. **Lua脚本保证原子性**：INCR + EXPIRE 是原子操作，避免并发下计数与过期不一致
2. **按用户维度限流**：Key 格式 `ecommerce:ai:ratelimit:{userId}`
3. **配置项**：
   - `ecommerce.ai.rate-limit.max-requests`：最大请求数，默认10
   - `ecommerce.ai.rate-limit.time-window-seconds`：时间窗口秒数，默认60
4. **降级策略**：Redis异常时放行（返回true），避免限流组件故障影响业务

**Lua脚本逻辑**：
```lua
local current = redis.call('INCR', KEYS[1])
if current == 1 then
    redis.call('EXPIRE', KEYS[1], ARGV[1])
end
return current
```

---

## 八、config 层 — 配置类

### 8.1 HttpClientConfig（HTTP客户端配置）

**功能**：配置 RestTemplate Bean，用于调用 DeepSeek AI API

**配置项**：
- `deepseek.api.timeout-seconds`：超时时间，默认30秒
- 同时设置连接超时和读取超时

### 8.2 Knife4jConfig（API文档配置）

**功能**：配置 SpringDoc OpenAPI，集成 Knife4j UI

**文档信息**：
- 标题：轻量化电商订单后端系统 API
- 版本：1.0.0
- 描述：基于 JDK 17 + Spring Boot 3.x + MyBatis-Plus 的电商后端系统

### 8.3 MybatisPlusConfig（MyBatis-Plus配置）

**核心配置**：

1. **Mapper扫描**：`@MapperScan("com.ecommerce.mapper")`
2. **分页插件**：`PaginationInnerInterceptor`（MySQL 方言）
3. **乐观锁插件**：`OptimisticLockerInnerInterceptor`
4. **自动填充处理器**：
   - 插入时填充 `createTime` 和 `updateTime`
   - 更新时填充 `updateTime`
   - 使用 `LocalDateTime.now()` 作为时间源

### 8.4 RedisConfig（Redis配置）

**核心配置**：

1. **RedisTemplate**：`RedisTemplate<String, Object>`
2. **序列化策略**：
   - Key：StringRedisSerializer
   - Value：Jackson2JsonRedisSerializer（JSON序列化）
3. **Jackson配置**：
   - 注册 JavaTimeModule（支持 LocalDateTime 序列化）
   - 日期格式：`yyyy-MM-dd HH:mm:ss`
   - 激活默认类型（`activateDefaultTyping`），用于反序列化时保留类型信息

---

## 九、enums 层 — 枚举类

### 9.1 OrderStatusEnum（订单状态枚举）

**枚举值**：

| 枚举 | code | text |
|------|------|------|
| PENDING_PAYMENT | 0 | 待支付 |
| PAID | 1 | 已支付 |
| SHIPPED | 2 | 已发货 |
| COMPLETED | 3 | 已完成 |
| CANCELED | 4 | 已取消 |

**工具方法**：
- `getTextByCode(Integer code)`：根据code获取文本描述，null或未知返回"未知"
- `canCancel(Integer code)`：判断是否允许取消（只有待支付和已支付可取消）

---

## 十、convert 层 — 对象转换

### 10.1 ProductConvert（商品转换器）

**基于 MapStruct 编译期转换**

| 方法 | 源类型 | 目标类型 | 特殊处理 |
|------|--------|---------|---------|
| `addDTOToDO` | ProductAddDTO | ProductDO | 直接映射 |
| `updateDTOToDO` | ProductUpdateDTO | ProductDO | 直接映射 |
| `doToVO` | ProductDO | ProductVO | status -> statusText 转换 |
| `doListToVOList` | List\<ProductDO\> | List\<ProductVO\> | 批量转换 |
| `doToStockVO` | ProductDO | ProductStockVO | id->productId, name->productName, stock>0->hasStock, status->statusText |

**状态转换规则**：1->已上架，0->已下架，其他->未知

### 10.2 OrderConvert（订单转换器）

| 方法 | 源类型 | 目标类型 | 特殊处理 |
|------|--------|---------|---------|
| `doToVO` | OrderDO | OrderVO | status -> statusText（调用 OrderStatusEnum） |
| `doListToVOList` | List\<OrderDO\> | List\<OrderVO\> | 批量转换 |
| `doToDetailVO` | OrderDO | OrderDetailVO | status -> statusText |

### 10.3 UserAddressConvert（用户地址转换器）

| 方法 | 源类型 | 目标类型 | 特殊处理 |
|------|--------|---------|---------|
| `addDTOToDO` | UserAddressAddDTO | UserAddressDO | 直接映射 |
| `doToVO` | UserAddressDO | UserAddressVO | 拼接 fullAddress（省+市+区+详情） |
| `doListToVOList` | List\<UserAddressDO\> | List\<UserAddressVO\> | 批量转换 |

---

## 十一、mapper 层 — 数据访问层

所有 Mapper 均继承 MyBatis-Plus 的 `BaseMapper<T>`，获得通用 CRUD 能力。

### 11.1 ProductMapper

**自定义SQL**：

| 方法 | SQL | 用途 |
|------|-----|------|
| `decreaseStock` | `UPDATE product SET stock = stock - #{quantity} WHERE id = #{productId} AND stock >= #{quantity} AND deleted = 0` | 原子扣减库存（防超卖核心） |
| `increaseStock` | `UPDATE product SET stock = stock + #{quantity} WHERE id = #{productId} AND deleted = 0` | 原子回滚库存 |

**设计要点**：
- `decreaseStock` 中的 `stock >= quantity` 条件是数据库层面的乐观锁，返回影响行数=1表示成功，=0表示库存不足
- 注意加上 `deleted = 0` 条件，避免扣减已逻辑删除商品的库存

### 11.2 OrderMapper

继承 `BaseMapper<OrderDO>`，无自定义SQL，使用 MyBatis-Plus 通用方法完成订单CRUD。

### 11.3 UserMapper

继承 `BaseMapper<UserDO>`，无自定义SQL。

### 11.4 UserAddressMapper

继承 `BaseMapper<UserAddressDO>`，无自定义SQL。

### 11.5 AiAfterSupportMapper

继承 `BaseMapper<AiAfterSupportDO>`，无自定义SQL。

---

## 十二、整体架构总结

### 12.1 架构分层

```
Controller 层（接口入口 + 参数校验 + 响应封装）
     ↓
Service 层（业务逻辑 + 事务控制 + 分布式锁 + 缓存）
     ↓
Convert 层（MapStruct 对象转换，编译期生成代码）
     ↓
Mapper 层（MyBatis-Plus 数据访问 + 自定义SQL）
     ↓
Entity 层（数据库实体 DO）
```

### 12.2 核心技术亮点

| 技术点 | 应用场景 | 实现方式 |
|--------|---------|---------|
| 分布式锁防超卖 | 订单创建/取消 | Redisson + 按商品ID加锁 + 等待/租约时间 |
| DB乐观锁防超卖 | 库存扣减 | `stock >= quantity` 原子SQL判断 |
| 缓存策略 | 商品详情 | Cache-Aside 模式 + 主动失效 + 异常降级 |
| AI大模型集成 | 售后智能分析 | DeepSeek API + 强制JSON输出 |
| 限流保护 | AI服务调用 | Redis + Lua 滑动窗口计数器 |
| 降级机制 | AI服务调用 | 失败返回"待人工审核"，不影响主流程 |
| 全局异常处理 | 所有接口 | @RestControllerAdvice 统一处理 |
| 参数校验 | 所有接口 | Jakarta Validation + @Validated |
| API文档 | 所有接口 | Knife4j + SpringDoc OpenAPI |
| 逻辑删除 | 商品/订单/用户/地址 | MyBatis-Plus @TableLogic |
| 自动填充 | 所有业务表 | createTime / updateTime 自动填充 |

### 12.3 数据一致性保障

1. **订单与库存一致性**：通过 `@Transactional` 事务 + 分布式锁保证
2. **缓存与DB一致性**：写操作后主动删除缓存，读操作 Cache-Aside 模式
3. **订单快照机制**：商品名称/价格/地址下单时快照，防止后续变更影响历史订单

### 12.4 高可用设计

1. **Redis 异常降级**：缓存操作异常不影响主流程
2. **AI 服务降级**：大模型调用失败返回"待人工审核"
3. **限流组件降级**：限流Redis异常时放行请求
4. **全局异常兜底**：未捕获异常统一返回系统错误
